/* Mobile input adapter: direct screen touch/drag controls movement intent. No virtual joystick. */
(()=>{let pointerId=null,startX=0,startY=0,lastX=0,lastY=0;const DEADZONE=3,MAX_DRAG=90;function apply(x,y){lastX=x;lastY=y;const dx=x-startX,dy=y-startY,len=Math.hypot(dx,dy);if(len<DEADZONE){InputSystem.set({moveX:0,moveY:0});return}const scale=Math.min(1,len/MAX_DRAG),nx=dx/len,ny=dy/len;InputSystem.set({moveX:nx*scale,moveY:ny*scale})}function down(e){if(pointerId!==null||e.target.closest('button'))return;pointerId=e.pointerId;startX=lastX=e.clientX;startY=lastY=e.clientY;document.body.setPointerCapture?.(e.pointerId);InputSystem.set({moveX:0,moveY:0})}function move(e){if(e.pointerId===pointerId)apply(e.clientX,e.clientY)}function up(e){if(e.pointerId!==pointerId)return;pointerId=null;InputSystem.set({moveX:0,moveY:0})}const system={id:'touch-input',dependsOn:['input'],start(){document.body.style.touchAction='none';addEventListener('pointerdown',down,{passive:false});addEventListener('pointermove',move,{passive:false});addEventListener('pointerup',up);addEventListener('pointercancel',up)},stop(){removeEventListener('pointerdown',down);removeEventListener('pointermove',move);removeEventListener('pointerup',up);removeEventListener('pointercancel',up)}};GameSystems.register(system);window.TouchInputAdapter=system})();