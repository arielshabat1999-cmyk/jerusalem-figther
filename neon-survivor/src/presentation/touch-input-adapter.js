/* Legacy-parity mobile flight controls: drag anywhere and the ship follows finger displacement. */
(()=>{let pointerId=null,lastX=0,lastY=0,targetX=0,targetY=0,originX=0,originY=0;const X_GAIN=1.16,Y_GAIN=1.08;function player(){const s=SessionSystem.snapshot();return WorldSystem.findByPlayerId(s.playerId)}function down(e){if(pointerId!==null||e.pointerType==='mouse'||e.target.closest('button'))return;const p=player();if(!p||!RunCommands.snapshot()?.active)return;pointerId=e.pointerId;lastX=e.clientX;lastY=e.clientY;targetX=p.transform.x;targetY=p.transform.y;originX=p.transform.x;originY=p.transform.y;InputSystem.set({moveX:0,moveY:0,targetX,targetY,directFollow:true});e.preventDefault()}function move(e){if(e.pointerId!==pointerId)return;const dx=e.clientX-lastX,dy=e.clientY-lastY;lastX=e.clientX;lastY=e.clientY;targetX=Math.max(originX-innerWidth/2+26,Math.min(originX+innerWidth/2-26,targetX+dx*X_GAIN));targetY=Math.max(originY-innerHeight*.16,Math.min(originY+innerHeight*.37,targetY+dy*Y_GAIN));InputSystem.set({targetX,targetY,directFollow:true,moveX:0,moveY:0});e.preventDefault()}function up(e){if(e.pointerId!==pointerId)return;pointerId=null;InputSystem.set({moveX:0,moveY:0,directFollow:false})}const system={id:'touch-input',dependsOn:['input','world','session','run'],start(){document.body.style.touchAction='none';addEventListener('pointerdown',down,{passive:false});addEventListener('pointermove',move,{passive:false});addEventListener('pointerup',up);addEventListener('pointercancel',up)},stop(){removeEventListener('pointerdown',down);removeEventListener('pointermove',move);removeEventListener('pointerup',up);removeEventListener('pointercancel',up)}};GameSystems.register(system);window.TouchInputAdapter=system})();