(()=>{let hud,rankBox;function rank(p){if(!rankBox)return;const os=p.objectives||[];rankBox.innerHTML=`<b>RANK ${p.rank}</b><small>${os.filter(o=>o.complete).length} / ${os.length} OBJECTIVES</small>`}function stats(){if(!hud)return;const r=RunSystem.snapshot(),eco=EconomySystem.snapshot();hud.querySelector('.stats').textContent=`SCORE ${Math.floor(r.score||0)}   LEVEL ${r.level||1}   COINS ${eco.balances.coins||0}`};const system={id:'hud',dependsOn:['run','rank','economy'],start(){hud=document.createElement('div');hud.innerHTML='<div class="stats"></div><button class="rank"></button><div class="objectives"></div>';Object.assign(hud.style,{position:'fixed',inset:'0',zIndex:'20',pointerEvents:'none',color:'#eaffff',fontFamily:'system-ui'});document.body.appendChild(hud);rankBox=hud.querySelector('.rank');Object.assign(rankBox.style,{position:'absolute',left:'14px',top:'14px',padding:'10px 14px',border:'1px solid #67e8f977',background:'#06101acc',color:'inherit',pointerEvents:'auto'});rankBox.style.display='flex';rankBox.style.flexDirection='column';const st=hud.querySelector('.stats');Object.assign(st.style,{position:'absolute',top:'18px',left:'50%',transform:'translateX(-50%)',fontWeight:'800'});const obj=hud.querySelector('.objectives');Object.assign(obj.style,{display:'none',position:'absolute',left:'50%',top:'50%',transform:'translate(-50%,-50%)',width:'min(88vw,420px)',padding:'20px',border:'1px solid #67e8f977',background:'#06101aee',borderRadius:'20px',pointerEvents:'auto'});rankBox.onclick=()=>{const p=GameState.get().progression,os=p.objectives||[];obj.innerHTML='<h2>OBJECTIVES</h2>'+os.map(o=>`<p>${o.complete?'✓':'○'} ${o.value} / ${o.target} — ${o.metric}</p>`).join('');obj.style.display=obj.style.display==='none'?'block':'none'};GameEvents.on('rank:progress',rank);GameEvents.on('run:score-changed',stats);GameEvents.on('run:level-changed',stats);GameEvents.on('economy:balance-changed',stats);stats()}};GameSystems.register(system);window.HUDSystem=system})();