(()=>{let running=false,last=0,acc=0,raf=0;const STEP=1/60,MAX=.25;function frame(now){if(!running)return;const dt=Math.min(MAX,(now-last)/1000||0);last=now;acc+=dt;while(acc>=STEP){GameEvents.emit('simulation:tick',{dt:STEP});acc-=STEP}GameEvents.emit('presentation:frame',{alpha:acc/STEP,now});raf=requestAnimationFrame(frame)}window.GameClock=Object.freeze({start(){if(running)return;running=true;last=performance.now();acc=0;raf=requestAnimationFrame(frame)},stop(){running=false;cancelAnimationFrame(raf)},get running(){return running},step:STEP})})();