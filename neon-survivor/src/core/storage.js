/* Namespaced persistent storage with schema versions. */
window.GameStorage=Object.freeze({read(key,fallback=null){try{const raw=localStorage.getItem('NS:'+key);return raw==null?fallback:JSON.parse(raw)}catch(e){console.warn('[GameStorage] read',key,e);return fallback}},write(key,value){try{localStorage.setItem('NS:'+key,JSON.stringify(value));return true}catch(e){console.error('[GameStorage] write',key,e);return false}},remove(key){try{localStorage.removeItem('NS:'+key)}catch(e){console.warn('[GameStorage] remove',key,e)}},migrate(key,currentVersion,migrations={}){let box=this.read(key,{version:0,data:null});while(box.version<currentVersion){const next=box.version+1,fn=migrations[next];if(typeof fn!=='function')throw new Error('Missing migration '+key+' v'+next);box={version:next,data:fn(box.data)}}this.write(key,box);return box.data}});
