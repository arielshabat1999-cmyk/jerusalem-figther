(()=>{const velocities=new Map();const system={id:'movement',dependsOn:['world'],start(){GameEvents.on('entity:despawned',({entity})=>velocities.delete(entity.entityId))},setVelocity(entityId,x=0,y=0){velocities.set(entityId,{x:+x||0,y:+y||0})},tick(dt){for(const [id,v] of velocities){const e=WorldSystem.get(id);if(!e)continue;e.transform.x+=v.x*dt;e.transform.y+=v.y*dt;GameEvents.emit('entity:moved',{entityId:id,transform:{...e.transform}})}},stop:id=>velocities.delete(id)};GameSystems.register(system);window.MovementSystem=system})();