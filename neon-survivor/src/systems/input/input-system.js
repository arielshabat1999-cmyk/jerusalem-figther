(()=>{const state={moveX:0,moveY:0,fire:false,aimX:0,aimY:-1};let lastSent=0,lastNet={moveX:0,moveY:0};function networkMovement(now=performance.now()){if(!window.NetworkCommandSystem)return;const dx=Math.abs(state.moveX-lastNet.moveX),dy=Math.abs(state.moveY-lastNet.moveY);if(now-lastSent<50&&dx<.04&&dy<.04)return;lastSent=now;lastNet={moveX:state.moveX,moveY:state.moveY};NetworkCommandSystem.submit('move',{moveX:state.moveX,moveY:state.moveY})}const system={id:'input',dependsOn:[],start(){},set(patch={}){Object.assign(state,patch);GameEvents.emit('input:changed',{...state});if('moveX'in patch||'moveY'in patch)networkMovement()},snapshot:()=>({...state}),reset(){Object.assign(state,{moveX:0,moveY:0,fire:false,aimX:0,aimY:-1});lastNet={moveX:0,moveY:0};lastSent=0}};GameSystems.register(system);window.InputSystem=system})();