(()=>{const SAVE='player.inventory';let owned={},equipped={};const persist=()=>GameStorage.write(SAVE,{version:1,data:{owned,equipped}});const system={id:'inventory',dependsOn:[],start(){const b=GameStorage.read(SAVE);if(b?.version===1){owned=b.data?.owned||{};equipped=b.data?.equipped||{}}},add(itemId,quantity=1,meta={}){if(!itemId)throw new Error('itemId required');const row=owned[itemId]||{quantity:0,meta:{}};row.quantity+=Math.max(1,quantity|0);row.meta={...row.meta,...meta};owned[itemId]=row;persist();GameEvents.emit('inventory:item-added',{itemId,quantity,row:{...row}});return row},owns:id=>(owned[id]?.quantity||0)>0,equip(slot,itemId){if(!system.owns(itemId))throw new Error('Item not owned: '+itemId);equipped[slot]=itemId;persist();GameEvents.emit('inventory:item-equipped',{slot,itemId});return itemId},snapshot:()=>structuredClone({owned,equipped})};GameSystems.register(system);window.InventorySystem=system})();