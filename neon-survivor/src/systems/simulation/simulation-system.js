(()=>{let elapsed=0,lastX=0,lastY=0,totalDistance=0;const system={id:'simulation',dependsOn:['input','movement','enemy-ai','boss-patterns','auto-fire','collision','run'],start(){GameEvents.on('run:started',()=>{elapsed=0;totalDistance=0;lastX=0;lastY=120});GameEvents.on('simulation:tick',({dt})=>system.tick(dt))},tick(dt){const current=RunCommands.snapshot();if(!current?.active)return;elapsed+=dt;const session=SessionSystem.snapshot(),player=WorldSystem.findByPlayerId(session.playerId);if(player){const i=InputSystem.snapshot(),speed=(player.data.speed||1)*220;MovementSystem.setVelocity(player.entityId,i.moveX*speed,i.moveY*speed)}EnemyAISystem.tick(dt);BossPatternSystem.tick(dt);MovementSystem.tick(dt);if(player){const dx=player.transform.x-lastX,dy=player.transform.y-lastY,delta=Math.hypot(dx,dy);totalDistance+=delta;lastX=player.transform.x;lastY=player.transform.y;RunCommands.distance(totalDistance)}CollisionSystem.tick();AutoFireSystem.tick();RunCommands.time(elapsed)}};GameSystems.register(system);window.SimulationSystem=system})();