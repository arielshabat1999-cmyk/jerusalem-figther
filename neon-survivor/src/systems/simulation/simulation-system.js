(()=>{let elapsed=0;const system={id:'simulation',dependsOn:['input','movement','enemy-ai','auto-fire','run'],start(){GameEvents.on('run:started',()=>{elapsed=0});GameEvents.on('simulation:tick',({dt})=>system.tick(dt))},tick(dt){if(!RunSystem.snapshot().active)return;elapsed+=dt;const session=SessionSystem.snapshot(),player=WorldSystem.list().find(e=>e.playerId===session.playerId);if(player){const i=InputSystem.snapshot(),speed=(player.data.speed||1)*220;MovementSystem.setVelocity(player.entityId,i.moveX*speed,i.moveY*speed)}EnemyAISystem.tick(dt);MovementSystem.tick(dt);AutoFireSystem.tick();RunCommands.time(elapsed)}};GameSystems.register(system);window.SimulationSystem=system})();