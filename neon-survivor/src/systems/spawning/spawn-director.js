(()=>{let t=0,next=0;const system={id:'spawn-director',dependsOn:['spawning','run'],start(){GameEvents.on('run:started',()=>{t=0;next=.5});GameEvents.on('simulation:tick',({dt})=>system.tick(dt))},tick(dt){if(!RunSystem.snapshot().active)return;t+=dt;if(t<next)return;const difficulty=1+Math.floor(t/60),angle=Math.random()*Math.PI*2,radius=360+Math.random()*140;SpawnSystem.enemy(difficulty>=5&&Math.random()<.08?'elite':'scout',{x:Math.cos(angle)*radius,y:Math.sin(angle)*radius});next=t+Math.max(.25,1.25-difficulty*.08)}};GameSystems.register(system);window.SpawnDirector=system})();