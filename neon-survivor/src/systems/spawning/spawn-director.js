(()=>{let t=0,next=0;const system={id:'spawn-director',dependsOn:['spawning','run','world','session'],start(){GameEvents.on('run:started',()=>{t=0;next=.5});GameEvents.on('simulation:tick',({dt})=>system.tick(dt))},tick(dt){if(!RunCommands.snapshot()?.active)return;t+=dt;if(t<next)return;const session=SessionSystem.snapshot(),player=WorldSystem.list().find(e=>e.playerId===session.playerId&&!e.health?.dead);if(!player)return;const difficulty=1+Math.floor(t/60),angle=Math.random()*Math.PI*2,radius=360+Math.random()*140,x=player.transform.x+Math.cos(angle)*radius,y=player.transform.y+Math.sin(angle)*radius;SpawnSystem.enemy(difficulty>=5&&Math.random()<.08?'elite':'scout',{x,y});next=t+Math.max(.25,1.25-difficulty*.08)}};GameSystems.register(system);window.SpawnDirector=system})();