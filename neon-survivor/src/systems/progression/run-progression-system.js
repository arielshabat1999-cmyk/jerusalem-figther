(()=>{let xp=0;const levelFor=x=>1+Math.floor(Math.sqrt(Math.max(0,x)/100));const system={id:'run-progression',dependsOn:['run'],start(){GameEvents.on('run:started',()=>{xp=0;RunCommands.score(0);RunCommands.level(1)});GameEvents.on('combat:entity-killed',({kind})=>{if(!RunSystem.snapshot().active)return;const gain=kind==='boss'?500:kind==='elite'?100:10;xp+=gain;const r=RunSystem.snapshot();RunCommands.score((r.score||0)+gain);RunCommands.level(levelFor(xp));GameEvents.emit('run:xp-changed',{xp,level:levelFor(xp)})})},snapshot:()=>({xp,level:levelFor(xp)})};GameSystems.register(system);window.RunProgressionSystem=system})();