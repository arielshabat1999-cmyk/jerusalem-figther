(()=>{const phases=new Map();function nearest(e,players){let target=null,best=Infinity;for(const p of players){const dx=p.transform.x-e.transform.x,dy=p.transform.y-e.transform.y,d=dx*dx+dy*dy;if(d<best){best=d;target=p}}return{target,distance:Math.sqrt(best)}}function velocity(e,target,distance,now){const dx=target.transform.x-e.transform.x,dy=target.transform.y-e.transform.y,len=Math.hypot(dx,dy)||1,nx=dx/len,ny=dy/len,speed=(e.data.speed||1)*100,role=e.data.combatRole||'striker',range=e.data.preferredRange||0;let vx=nx*speed,vy=ny*speed;if(role==='sweeper'){const phase=phases.get(e.entityId)||((e.entityId.length%7)*.7);vx=(nx*.58+Math.cos(now*.0022+phase)*.82)*speed;vy=(ny*.72+Math.sin(now*.0017+phase)*.24)*speed}else if(role==='phantom'){const phase=phases.get(e.entityId)||((e.entityId.length%5)*.9);const pulse=.72+.35*Math.sin(now*.004+phase);vx=(nx*.76+Math.cos(now*.003+phase)*.38)*speed*pulse;vy=(ny*.76+Math.sin(now*.0024+phase)*.22)*speed*pulse}else if(range){let move=1;if(distance<range*.72)move=-.65;else if(distance<=range*1.05)move=0;vx=nx*speed*move;vy=ny*speed*move;if(role==='mortar'){vx+=-ny*speed*.16;vy+=nx*speed*.16}}return{vx,vy,rotation:Math.atan2(dy,dx)+Math.PI/2}}const system={id:'enemy-ai',dependsOn:['world','movement','weapons'],start(){GameEvents.on('entity:despawned',({entity})=>phases.delete(entity.entityId))},tick(dt,now=performance.now()){const players=WorldSystem.listByType('player').filter(e=>!e.health?.dead);if(!players.length)return;for(const e of WorldSystem.listByType('enemy')){if(e.health?.dead)continue;if(!phases.has(e.entityId))phases.set(e.entityId,Math.random()*Math.PI*2);const {target,distance}=nearest(e,players);if(!target)continue;if(e.weapons?.length&&distance<=Math.max((e.data.preferredRange||0)*1.35,420))for(const weaponId of e.weapons)WeaponSystem.fire({sourceId:e.entityId,weaponId,targetId:target.entityId,now});const m=velocity(e,target,distance,now);MovementSystem.setVelocity(e.entityId,m.vx,m.vy);e.transform.rotation=m.rotation}}};GameSystems.register(system);window.EnemyAISystem=system})();