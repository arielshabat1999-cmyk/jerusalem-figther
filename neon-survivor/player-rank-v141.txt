/* V145 — Player Rank modular bootstrap. Kept at legacy filename so index.html needs no invasive change. */
;(async()=>{
  try{
    const loadText=async f=>{const r=await fetch('./'+f+'?v=20260920v145',{cache:'no-store'});if(!r.ok)throw new Error(f+' '+r.status);return r.text()};
    const config=await loadText('player-rank-config-v143.js');
    (0,eval)(config);
    const renderer=await loadText('player-rank-renderer-v145.txt');
    (0,eval)(renderer);
    const tracker=await loadText('player-rank-tracker-v144.txt');
    (0,eval)(tracker);
    console.log('PLAYER RANK V145 READY');
  }catch(e){console.error('PLAYER RANK V145 FAILED',e)}
})();